# requirements.txt
# Core scientific stack
numpy>=1.24
scipy>=1.11
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12

# Machine learning
scikit-learn>=1.3
torch>=2.0          # MartingaleSchrodingerBridge neural network

# Market data
yfinance>=0.2.28

# Optimisation
tqdm>=4.65

# Serialisation / config
PyYAML>=6.0
pyarrow>=12.0       # parquet support for raw option data

# Testing
pytest>=7.4
pytest-cov>=4.1

# Optional — Interactive Brokers live feed
# ib_insync>=0.9.86

# Optional — Latin Hypercube sampling (used in GP calibration)
# Already included in scipy.stats.qmc (scipy >= 1.7)
